"""Tests for src/evaluation/ — WER, UTMOS, speaker SIM, benchmark.

Tests to implement in Phase 6:
    - WER scorer returns float in [0, 1] for a sample transcription
    - UTMOS scorer returns float in expected range
    - Speaker SIM uses ECAPA-TDNN (not raw cosine on speaker embeddings)
    - Benchmark runs on 5 test sentences and writes output JSON
"""

import pytest


def test_placeholder():
    """Placeholder — replace with real tests in Phase 6."""
    pass
